# Base URL of the backend API.
# Local dev: leave BLANK — Vite proxies /api and /uploads to localhost:5000.
# Production (Vercel): set to your Render backend URL, with NO trailing slash:
#   VITE_API_URL=https://librenet-api.onrender.com
VITE_API_URL=
